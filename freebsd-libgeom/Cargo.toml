[package]
name = "freebsd-libgeom"
version = "0.1.0"
edition = "2018"
authors = ["Alan Somers <asomers@gmail.com>"]
license = "BSD-2-Clause"
description = "Rust bindings to FreeBSD's libgeom library"
categories = ["api-bindings", "os::freebsd-apis"]
keywords = ["freebsd"]

[dependencies]
freebsd-libgeom-sys = { version = "=0.1.0", path = "../freebsd-libgeom-sys" }
lazy_static = "1.4"

[dev-dependencies]
approx = "0.5.0"
nix = "0.20.0"
